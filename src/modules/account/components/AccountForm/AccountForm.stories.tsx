import React from 'react';
import { AccountForm } from '@onr/account';

export default {
  title: 'Modules|account/AccountForm',
};

export const accountForm = () => <AccountForm currentAccount={{}} />;
